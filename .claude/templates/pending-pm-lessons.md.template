# Pending PM Lessons (staging)

> In-flight PM observations before user approval. PM writes here at the moment of noticing; at natural session boundaries, PM reviews pending entries with the user. Approved entries promote to `~/.claude/pm-user-lessons.md` (user-interaction) or `.claude/adaptive-team-learned/pm-lessons.md` (project-PM). Rejected entries are deleted. This file is NOT the final home of any lesson.

Each pending entry:

    ## YYYY-MM-DD HH:MM — pending
    **Scope:** user-interaction | project-PM
    **Observation:** what you noticed, in one paragraph
    **Proposed lesson:** draft text in the standard lesson format
    **Trigger context:** turn / phase / event that surfaced this

---

(no pending entries)
